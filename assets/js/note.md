ShapeRenderer

- drawShape(), drawArrow(), drawMultilineText(), getShapeBounds()

HistoryManager

- saveHistory(), undo(), redo()

BackgroundRemoval

- SelfieSegmentation

- GrabCut (OpenCV)

ImageLoader

- openImage()

- enableClipboardPaste()

CanvasTransform

- getDistance(), getCenter(), redrawWithTransform()

StickerManager